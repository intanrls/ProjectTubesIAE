# Log Aktivitas & Resume Kontribusi - IAE

**Nama:** Rahmandhika Muhammad El Kariem
**NIM:** 102022400047
**Kelompok:** 3
**Tugas:** IAE - Integrasi Aplikasi Enterprise

Dokumen ini berisi catatan progres pengerjaan modul *Inventory Service*, mencakup log bantuan teknis saat pengerjaan dan resume kontribusi individu selama tugas besar ini berlangsung.

---

## 1. Log Prompting AI
Catatan ini berisi ringkasan bantuan yang saya gunakan selama pengerjaan tugas untuk mempercepat proses *troubleshooting* dan referensi teknis.

Integrasi SOAPFormat XML audit sering *error* atau *typo*. Dibantu buat struktur `SimpleXMLElement` biar *tag* XML konsisten. 
RabbitMQ, Bingung *trigger* otomatis ke ekspedisi. Disarankan pakai `RabbitMQ Publisher` di *method* `storeQC`. 
API Debugging *Error* 404 pada detail barang. Pengecekan logika *query* (ternyata ketukar *ID*).
Docker *Service* gagal *connect* antar kontainer. *Re-check* konfigurasi *network bridge* di `docker-compose.yml`.
Dokumentasi *Setup* Swagger API. Dibantu cara pakai anotasi `@OA` agar *auto-generate*.

---

## 2. Resume Kontribusi Individu
Berikut adalah rangkuman pekerjaan teknis yang saya kerjakan secara mandiri untuk modul *Inventory Service*:

* **Pengembangan Fitur**:
    * Membangun *CRUD* utama untuk pengelolaan stok barang.
    * Implementasi logika pengecekan stok yang sinkron dengan *Order Service*.
* **Integrasi Sistem**:
    * **SOAP Audit**: Mengintegrasikan sistem dengan modul audit dosen menggunakan format XML.
    * **RabbitMQ (AMQP)**: Mengatur alur pesan otomatis saat barang lolos QC agar *Expedition Service* bisa langsung memproses data tanpa *request* manual.
* **Infrastruktur & Dokumentasi**:
    * Melakukan konfigurasi *Docker container* agar *service* bisa berkomunikasi dalam satu *network*.
    * Menyusun dokumentasi API menggunakan *L5-Swagger* untuk memudahkan integrasi antar *service* kelompok.
* **Kolaborasi**:
    * Melakukan sinkronisasi data dengan *Order Service*.
    * Melakukan *debugging* bersama tim saat ada kendala integrasi antar *service*.

---

## Refleksi
Pengerjaan tugas ini memberi pengalaman nyata tentang integrasi sistem yang kompleks. Kendala terbesar bagi saya adalah menyelaraskan protokol yang berbeda (REST, SOAP, dan RabbitMQ). Penggunaan alat bantu (seperti AI) sangat membantu saat saya mengalami kebuntuan/stuck pada syntax atau konfigurasi, sehingga progres pengerjaan bisa tetap berjalan sesuai *timeline* tanpa harus membuang waktu terlalu lama untuk mencari referensi dasar.